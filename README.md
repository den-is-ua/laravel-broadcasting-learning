# Broadcasting learning project

This repository is a small Laravel app for learning **event broadcasting**.

To try the **public channel**, run:

```bash
php artisan fire:status-notification {status}
```

Replace `{status}` with any string you want to send. Then open the app in the browser, keep the **developer console** open, and you should see the broadcast reflected there.
